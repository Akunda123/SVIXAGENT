# ══════════════════════════════════════════════════════════════════
# AKDAgent / SVIXAGENT —— 只把「源码 + 技能 + 知识 + 工具」放上库
# 原则：**构建产物、内嵌运行时、模型权重、安装包、工作台账** 一律不进库。
# （2026-09-23 立：补之前那份只忽略 4 行的 .gitignore ——
#   不改的话 `git add .` 会尝试提交 ~3.6 GB、含 12 个 >50MB 的文件，
#   其中 electron/release 的安装包 377MB **超过 GitHub 的 100MB 硬上限、push 必被拒**。）
# ══════════════════════════════════════════════════════════════════

# ── 依赖 ──────────────────────────────────────────────────────────
node_modules/
**/node_modules/

# ── Python 字节码（scripts/*.py 跑一次就会生成）────────────────────
__pycache__/
*.pyc

# ── 构建产物 ──────────────────────────────────────────────────────
dist/
**/dist/
server/dist/
*.tsbuildinfo
electron/release/
electron/out/

# ── 内嵌运行时 / 二进制（几十~几百 MB，且属上游产物）──────────────
dsh-runtime/
dsh-runtime/node/
dsh-runtime/dsh/
dsh.*.bak-*/

# ── 模型权重（CREPE / 分离模型，64~85 MB 起；GitHub 会拒）──────────
server/models/
**/models/
*.onnx

# ── 安装包 / 压缩包 / 打包中间物 ─────────────────────────────────
*.exe
*.msi
*.dmg
*.zip
*.7z
*.blockmap
*.asar
*.nupkg

# ── 音频 / 临时产物 ──────────────────────────────────────────────
*.wav
*.mp3
*.log
_tmp*.wav
.DS_Store
Thumbs.db

# ── 本仓工作台账（内部记录：事故/审议/进度，**不对外**）──────────
#    ⚠️ 若要公开开发过程，删掉这一段即可（但建议先跑 tools/check-redaction.cjs）
#    ⚠️⚠️ 前导斜杠不能省：写 `docs/` 会按「任意层级的 docs」匹配，
#        把**随包分发的知识包 `knowledge/docs/`（13 个文件）**一起挡掉（2026-09-24 体检抓到）。
/docs/
README-发布版草案.md
README-发布版草案-v2.md

# ── 备份 / 历史快照 ──────────────────────────────────────────────
*.bak
*.bak-*
*_bak/
backup*/
*.orig

# ── 密钥 / 本地配置（**绝不上库**）────────────────────────────────
*.key
*.pem
.env
.env.*
secrets*.json
config.local.*

# ── 整包步骤书（tools/pack-mac-payload.ps1 生成 → 打进 zip → 自己删掉）──
MAC-STEPS.md
