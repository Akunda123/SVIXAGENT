# SV 脚本 API 参考

> 由 Synthesizer V Studio Scripting Manual 的 HTML 文档转换而来（内容原封不动）。

> 每个类一个子 md；全局对象 `SV` 与工程结构见对应文件。
>
> ⚠️ **本目录是"官方手册镜像"，不要往里写自己的东西** —— `tools/api-docs-sync.cjs --update` 会**整批重写**这些类 md（它只排除本 README），而且它是**整文件覆盖、零保留**：写在这儿的注解会被**静默抹掉**。
> 🔴 **2026-09-20 实测教训**：这里曾经压着 **258 行**我们自己的实测注解（`Note.md` **244 行** + `SV.md` **12 行**）—— 一次 `--update` 就会全没。已搬到 **[references/官方API-实测勘误.md](../references/官方API-实测勘误.md)**，并由 `tools/check-api-mirror.cjs` **守住纯净度**（镜像里出现 `实测补充`/`akdagent`/`真机`/`⇒` 等本地标记即 exit 1）。
> 自研内容放别处：**磁盘工程文件字段**（`.svp`/`.ixp` 的 JSON 结构、automation 扁平数组、recovery 目录、安全读写）→ **`skills/sv-project-format/SKILL.md`**；
> 桥/通道协议 → `skills/akdagent-protocol/SKILL.md`；脚本实践与踩坑 → `skills/sv-scripting/references/官方API-实测勘误.md` · `skills/sv-scripting/references/`（01–11 编号正文）· `skills/akdagent-playbook/`。

> ⭐ **调用 / 探测任何宿主 API 之前，先在这里查签名**（2026-09-20 新增规则）—— 一条命令：
> `node tools/api-lookup.cjs <Class>#<method>`（列类：`api-lookup.cjs <Class>` · 不知道在哪个类：`--search <关键词>`）。
> ⛔ **别用"真机试错"确认签名**：试错在 SV1 上会弹模态框**冻死桥**（`known-bugs` 的 `SV-001`）；
> 2026-09-20 正因没查这里，把 `Automation.getPoints(begin, end)`（**本文档连返回结构例子都给了**）误判成"序列化卡死"。

## 🔄 版本与同步（2026-09-13 起）

**站点**：<https://resource.dreamtonics.com/scripting/index.html>
**判据**：页脚那一行 —— `Documentation generated by JSDoc x.y.z on <时间戳>`。**时间戳变了 ⇒ 文档重生成过**（比"逐页 diff"稳得多）。
**基线**：`_sync.json`（同目录）—— 记录 `generator` / `generatedAt` / 类清单 / 各宿主版本快照。

当前基线：

| 项 | 值 |
|---|---|
| 生成器 | JSDoc 4.0.4 |
| **生成时间戳** | **Thu Oct 09 2025 18:00:39 GMT+0900 (Japan Standard Time)** |
| 类数 | 23（index 页链接 20 + 站点上另存 3：`ArrangementSelectionState` / `ScriptableNestedObject` / `TrackInnerSelectionState`） |
| **非类页面** | **2** —— `Home.md`（手册首页）· `Tutorials.md`（5 篇教程）。⚠️ **2026-09-19 才补上**：此前只镜像了类页，**首页与教程全缺**（用户报「Home 等内容不见了」）；这两份**不由 `--update` 抓取**（见「非类页面」节）。 |
| 宿主版本（真机实测） | **SV1 1.11.2** · **SV2 2.2.1** · **IX 1.0.0** |

**本地官方手册存档**（Home/教程的来源，`--update` 用不到它）：`C:\Users\<USER>\Downloads\sv script\`
—— `*.html`（29 页：23 类 + Home + 5 教程）+ 4 份 `_api_*.md` 主题摘要。

**怎么用**（工具：`tools/api-docs-sync.cjs`）：

```bash
```

**自动化的那一环**：Electron 主进程 `app.whenReady()` 里已挂 `checkApiDocsVersion()` ——
它跑 `--startup` 做**离线**比对（各变体的现役版本 vs 包内基线），**联网抓页脚 / 重写文档只在开发侧手动跑**
（2026-09-14 分发政策：客户端不出网、不自动检测更新）；离线 / 桥没起 / 工具缺失都只打日志，不影响启动。
（⚠️ 打包版按 `extraResources` 带上 `tools/`（知识包 = `knowledge/docs` + `tools`）；没带则该自检跳过。）

> ⚠️ **`_sync.json` 的 `generatedAt` 是"文档内容版本"的唯一权威**：内容没重生成时，即使宿主小版本号变了也不必动文档。

## 非类页面（**不从站点抓，本地存档导入**）

- [Home](Home.md) —— 手册首页：支持语言（JS ES5.1 / Lua 5.4）· 能力清单 · 怎么上手 · 编程模型 · **JS 与 Lua 的差异**（索引 0/1 起、`Class.Method` vs `Class:Method`）
- [Tutorials](Tutorials.md) —— 5 篇教程（**含 JS + Lua 两版代码**）：A Minimal Example · Custom Dialogs · Custom Side Panel Sections · Localization · Memory Management

> ⚠️ 这两份的**更新方式不同**于类页：`--update` 只重写 23 个类 md（站点上它们是 `Xxx.html` 的类页；首页是 `index.html`、教程是 `tutorial-*.html`，URL 形态与转换口径都不同）。
> 要刷新它们 ⇒ 用本地存档（`Downloads\sv script\`）重新导入：Home 从 `Home - …html` 的 `<article>` 转换、Tutorials 从 `Downloads\sv script\_api_base_tutorials.md` 的 `# Tutorials` 段抽出。
> `tools/api-docs-sync.cjs` 已把这两个文件名列入 `NON_CLASS`，**不会**把它们当"本地多出来的类"报漂移。

## 类索引

- [ArrangementSelectionState](ArrangementSelectionState.md)
- [ArrangementView](ArrangementView.md)
- [Automation](Automation.md)
- [CoordinateSystem](CoordinateSystem.md)
- [GroupSelection](GroupSelection.md)
- [MainEditorView](MainEditorView.md)
- [NestedObject](NestedObject.md)
- [Note](Note.md)
- [NoteGroup](NoteGroup.md)
- [NoteGroupReference](NoteGroupReference.md)
- [PitchControlCurve](PitchControlCurve.md)
- [PitchControlPoint](PitchControlPoint.md)
- [PlaybackControl](PlaybackControl.md)
- [Project](Project.md)
- [RetakeList](RetakeList.md)
- [ScriptableNestedObject](ScriptableNestedObject.md)
- [SelectionStateBase](SelectionStateBase.md)
- [SV](SV.md)
- [TimeAxis](TimeAxis.md)
- [Track](Track.md)
- [TrackInnerSelectionState](TrackInnerSelectionState.md)
- [TrackMixer](TrackMixer.md)
- [WidgetValue](WidgetValue.md)
