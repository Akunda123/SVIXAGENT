/**
 * 音素近似数据的**「我们的修订层」**（2026-09-22 起）
 *
 * 真源 = `docs/ref-音素替换.js`（用户按**听感**手工分类的 `Vowels` / `Vowels0` / `Consonants0`；
 * 该副本**逐字留档、我们不改**）。本文件只放**我们提议的增补**，由 `tools/gen-phoneme-data.cjs`
 * 合并进产物 `server/src/phoneme/data.ts`：
 *   ① 原 js 保持可逐字核对；② 我们的修订单独一层、带理由、可回退；③ 用户以后更新 js 也不会把这些补充冲掉。
 *
 * 用户 2026-09-22 明确：「这个 Vowels 数组是我当时根据听感手动分类的，**可以修改**」。
 */
module.exports = {
  /**
   * 把"落不进任何族"的音素补进最贴近的族（`classes` 的键 = 0 起的族号，与参考 js 的 6 族对齐）。
   *
   * 背景（实测）：参考 js 里 **mandarin `j`** · **japanese `u`** · **cantonese `y`** 只出现在 `Vowels0`（全集），
   * 不在任何 `Vowels[k]` 里 ⇒ 原脚本给这三个音素的候选是**空 ComboBox**（等于没得选）。
   * 用户口径（2026-09-22）：「这次是 AI 不是写死的数据，之前 js 的空内容和只有一项的内容你根据实际提供候选」。
   * 下面是**按音理的提议**（待用户听感复核）：
   */
  addToVowelClasses: {
    mandarin: { 2: ['j'] },    // j = 硬腭近音/介音 ⇒ 归 i 族（前高）
    japanese: { 5: ['u'] },    // u = 后高元音 ⇒ 归 u 族（日语 u 实际偏央，此归类可再议）
    cantonese: { 2: ['y'] },   // y = 前高圆唇 ⇒ 归 i 族（前高）
  },
  /**
   * **引擎实测观察到的音素**（真机探针得来，补进"表内"合法集）。
   * 来历（2026-09-22 · SV2 2.2.1 · `getComputedAttributesForGroup` 实测）：
   *   韩语 `ja` 的 T2P 实际吐出 **`dz\`** —— 而**官方音素表与参考脚本清单都没有它**
   *   ⇒ 不补的话，我们"表外拒写"会**误拒一个引擎真在用的音素**。
   * （同一轮探针还确认：韩语 `ppa/tta/kka` 吐的是官方写法 `p_t`/`t_t`/`k_t`；
   *   参考脚本里的 `pp`/`tt` 是**另一种可接受的写法** —— 实测写 `pp 6` 后 computed 就是 `pp 6`，
   *   引擎**不归一化**、照收 ⇒ 拒写取并集是对的，但**候选排序应优先官方写法**。）
   */
  addAllowedSymbols: {
    korean: { consonants: ['dz\\'] },
  },
  /**
   * 我们**没有**改动的地方（写明，免得以后被误当成遗漏）：
   *   · 各族里其它语言的**空数组**：用户说明「第一个索引的空数组是因为我没在其他语种里找到类似发音」
   *     ⇒ 是**刻意留空**（该语种没有听感上的对应音），不是漏数据；生成器不做补齐。
   *   · `Vowels0` / `Consonants0` 与官方表的差集（korean `pp`/`tt` 只在参考表等）：
   *     ✅ 2026-09-22 真机已裁 —— **引擎用的是官方写法**（`p_t`/`t_t`/`k_t`），但**两种写法都接受**
   *     ⇒ 拒写取**并集**（`strict:"official"` 仍可切换），候选**优先官方**。
   */
  notes: [
    '修订只做「补进族」+「补引擎实测音素」；不删、不改用户原有分类。',
    '判定"落不进任何族"是由 tools/gen-phoneme-data.cjs --audit 打印出来的（Vowels0 减六族并集）。',
  ],
};
