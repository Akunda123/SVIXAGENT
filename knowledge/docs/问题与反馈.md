# 问题与反馈（人读版）

> **本文件由 `node tools/issue.cjs --digest` 从 `feedback/issues.json` 生成 —— 不要手改，改 JSON。**
> 生成时间：2026-09-22 11:57 · 共 3 条（未结 0）

## 怎么反馈

反馈只有一条路：**直接联系作者**（联系方式见 `README.md` 与客户端「关于」页）。
不需要开 issue、不需要导日志包、不需要 GitHub 账号 —— 说明现象与复现步骤即可，需要什么信息会当面问你。

> 作者的职责边界（2026-09-14 定案）：**只负责分发包**。新版本 = 重新分发安装包，不做自动更新、不做在线检测。

## 未结（0）

（暂无）
## 已结（3）

| ID | 日期 | 宿主 | 标题 | 结论 |
|---|---|---|---|---|
| `ISSUE-0001` | 2026-09-14 | Electron 客户端 | SV 集成目录里点「编辑」没反应 | ✅ 已解决：根因：编辑器 DOM 在列表重渲染时被 detach，而填充逻辑仍用 getElementById 取子节点 ⇒ 全部为 null，静默失败。修法：保存元素引用（flatEls）+ 先挂载再填充。已加 46/46 回归测试。 |
| `ISSUE-0002` | 2026-09-14 | Electron 客户端 | SV 集成目录留空时无法选目录 | ✅ 已解决：按用户澄清（"用浏览器的弹窗让用户选择目录"）实现：目录留空 或 点「浏览…」⇒ 调系统目录选择框（dialog.showOpenDialog，openDirectory + createDirectory）。 |
| `ISSUE-0003` | 2026-09-14 | Synthesizer V Studio | SV1 上调用不支持的 API 会弹框冻住桥 —— 用户裁定不算 bug | ⛔ 设计如此：用户 2026-09-14 裁定："SV1遇到不支持api弹窗应该不算bug，就是这么设计的"。⇒ 转为纪律：禁止试错式探测；存在性检查用「索引取值 + type() ∈ {function,userdata,table}」再 pcall 冒号调用。已登记 tools/known-bugs.json 的 SV-001（class=by-design，永不复检）。 |

## 记账纪律（内部）

用户一说 ⇒ `--new` 落档（=保存）→ 定位 `--status located` → 修完 `--status resolved --resolution "..."`
→ **回写 `skills/akdagent-playbook` 坑表**（=下次自动避开）→ 若属宿主缺陷再登记 `tools/known-bugs.json`（=带版本与复检触发）。

> 写入前默认脱敏（用户名 / 绝对路径 → 占位符）。